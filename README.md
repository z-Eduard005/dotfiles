## Install command:
```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/z-Eduard005/dotfiles/main/install.sh)"
```
*Or you can use stow for installing separate modules: `stow hypr waybar swaync` etc.*
